import 'package:flutter/material.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(child: Text("HOME HOME HOME"), color: Colors.blueGrey));
  }
}
/* 
앱 기본 화면:
카테고리2개 만든 후 각 
카테고리 별 물품 나오는 화면으로 전환
*/
