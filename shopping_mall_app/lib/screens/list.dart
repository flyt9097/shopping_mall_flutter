import 'package:flutter/material.dart';

class List extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(
            child: Text("LIST LIST LIST"), color: Colors.purpleAccent));
  }
}

//미구현 : 여러가지 목록들
